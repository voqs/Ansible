---

- name: Configure all prerequisites for Kubernetes via Ansible Pull
  hosts: 127.0.0.1
  connection: local
  gather_facts: yes
  become: yes
  roles:
    - zpoolMirror
    #- kubernetesPrerequisites
